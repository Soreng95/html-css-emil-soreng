ToDo:

x Product page
x Cart page

- Checkout page
- Success page
- Match images, price, name
- Add icons
- Meta tags on each page
  Test:
- Responsive
- All links

ToDo:
HTML must be semantic, valid (passing the W3C validator), and formatted. Each page must have a unique <title>, <meta name="description">, and <h1>.
CSS must follow DRY principles, be well-organized, and use appropriate selectors.

Check: The site must meet key WCAG standards: all images must have descriptive alt text, color contrast ratios must be sufficient, and the site must be keyboard-navigable.

The project must be hosted on GitHub Pages with a correctly named repository (html-css-<your-name>), and the Figma design must be publicly accessible to graders.

